# Claims audit - 2026-09-03

**Verdict: GREEN**  (11/11 verified)

- .. **ops-cadence guards empty price_date to avoid silent skip of staleness alert (addresses PR #710 review)** (`None`)
    - retracted: Superseded: warning wording changed from 'Could not extract' to 'Could not parse' in e1aadf5 and the guard was reworked; re-verified by later sessions (address-review-710-r2/r3).
- OK **docs-consistency workflow now also triggers on the four Markdown release-marker/skill inputs the release guard validates (AGENTS/CLAUDE/sdk-dev/SKILL)** (`file_contains`)
    - /skills/agentguard/SKILL.md/ found in .github/workflows/docs-consistency.yml
- OK **ops-cadence hardened to skip price-age check on empty OR unparseable LAST_UPDATED** (`file_contains`)
    - /Could not parse LAST_UPDATED/ found in .github/workflows/ops-cadence.yml
- OK **Reverted unjustified LAST_UPDATED bump; cost.py keeps true last-verified date 2026-05-19 (no false attestation)** (`file_contains`)
    - /LAST_UPDATED = "2026-05-19"/ found in sdk/agentguard/cost.py
- .. **ops-cadence surfaces a missing/unparseable LAST_UPDATED marker in the issue body, not just workflow logs** (`None`)
    - retracted: Original check pattern was malformed (shell/regex escaping of backticks); re-recording with a plain-text pattern.
- OK **ops-cadence issue body now flags a missing/unparseable price marker (not just workflow logs)** (`file_contains`)
    - /missing or unparseable/ found in .github/workflows/ops-cadence.yml
- OK **ops-cadence validates the LAST_UPDATED marker against a strict YYYY-MM-DD shape before calling date(1)** (`file_contains`)
    - /YYYY-MM-DD/ found in .github/workflows/ops-cadence.yml
- OK **Regenerated sdk/PYPI_README.md now includes the Security section from README.md (chromadb advisory)** (`file_contains`)
    - /## Security/ found in sdk/PYPI_README.md
- OK **sdk/PYPI_README.md documents the PYSEC-2026-311 chromadb advisory (in sync with README)** (`file_contains`)
    - /PYSEC-2026-311/ found in sdk/PYPI_README.md
- .. **Refreshed pricing-review date LAST_UPDATED in cost.py to 2026-09-03 (fixes 90-day staleness canary)** (`None`)
    - retracted: Superseded: the LAST_UPDATED bump was reverted in 03e82f7 after review (bumping without price re-verification was a false attestation). The canary is now fixed by the time-independent test, not a date bump.
- OK **New docs-consistency workflow runs PyPI README sync guard on doc/metadata changes that ci.yml skips** (`file_contains`)
    - /generate_pypi_readme.py --check/ found in .github/workflows/docs-consistency.yml
- OK **docs-consistency workflow also runs the release-metadata guard** (`file_contains`)
    - /sdk_release_guard.py/ found in .github/workflows/docs-consistency.yml
- OK **ops-cadence workflow nags via issue when price table exceeds 90 days since hand-verify** (`file_contains`)
    - /PRICE TABLE/ found in .github/workflows/ops-cadence.yml
- OK **test_last_updated_marker is time-independent (asserts date not in future, no 90-day time-bomb)** (`file_contains`)
    - /assertLessEqual\(updated, date.today\(\)\)/ found in sdk/tests/test_cost.py
