{
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- site/ api/",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/(.*)", "destination": "/site/$1" }
  ]
}
