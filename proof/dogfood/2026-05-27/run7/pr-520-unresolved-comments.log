THREAD 1: .github/workflows/release.yml:20 https://github.com/bmdhodl/agent47/pull/520#discussion_r3308466147
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Gate release creation on successful publishing**

When a release tag points at a commit whose `sdk/pyproject.toml` version matches but the separate `.github/workflows/publish.yml` job later fails its lint/tests/release-guard/build/PyPI upload, this workflow still creates the public GitHub Release because both workflows are independent `push`-tag triggers with no `needs`/`workflow_run` ordering. That can leave users and the release-content links pointing at an announced version that is not actually installable from PyPI.

Useful? React with 👍 / 👎.
---
THREAD 2: docs/RELEASING.md:33 https://github.com/bmdhodl/agent47/pull/520#discussion_r3308466150
**<sub><sub>![P2 Badge](https://img.shields.io/badge/P2-yellow?style=flat)</sub></sub>  Align prerelease docs with the publish guard**

Following this documented `vX.Y.Z-rc.N` path currently cannot publish successfully: `.github/workflows/publish.yml` runs `scripts/sdk_release_guard.py`, and that guard's release-marker regexes only accept stable `\d+\.\d+\.\d+` versions before comparing to the full project version. A prerelease package version such as `1.2.11-rc.1` therefore fails the metadata-sync gate before PyPI upload, even though this doc says prereleases are supported and the new release workflow will mark them as prereleases.

Useful? React with 👍 / 👎.
---
THREAD 3: .github/release.yml:21 https://github.com/bmdhodl/agent47/pull/520#discussion_r3308469469
The configured release-note labels do not match the repository's established label scheme. The issue templates apply `type:feature`, `type:bug`, and `type:docs` (for example, `.github/ISSUE_TEMPLATE/feature_request.yml:3` and `.github/ISSUE_TEMPLATE/bug_report.yml:3`), but these categories only match bare labels like `feature`, `bug`, and `docs`, so normally labeled PRs will fall into “Other changes”. Add the existing `type:*` labels here or update the repository labeling scheme consistently.
---
THREAD 4: docs/RELEASING.md:43 https://github.com/bmdhodl/agent47/pull/520#discussion_r3308469493
This documentation tells release managers to use bare labels, but the repository's issue templates use prefixed labels such as `type:feature`, `type:bug`, and `type:docs`. As written, following this guidance will not categorize PRs created with the existing labels; update the documented labels to match the release-note config after aligning it with the repo label scheme.

---
THREAD 5: .github/workflows/release.yml:44 https://github.com/bmdhodl/agent47/pull/520#discussion_r3308469515
This step only checks the tag string before creating the public GitHub Release, while the actual SDK release gates in `publish.yml` still run separately and can fail later. A stale CHANGELOG/PYPI README or failing tests could leave a GitHub Release visible for a package that was not successfully published. Reuse the release guard/tests here or create the GitHub Release only after the publish workflow succeeds.
---
THREAD 6: .github/release.yml:2 https://github.com/bmdhodl/agent47/pull/520#discussion_r3308469529
This comment says missing labels are handled by title prefix, but the generated-notes configuration below only defines label-based categories. Remove the title-prefix claim or add the mechanism that implements it so maintainers do not rely on behavior that will not happen.

---
